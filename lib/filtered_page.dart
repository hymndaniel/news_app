import 'package:flutter/material.dart';

class filtered_page extends StatefulWidget {
  @override
  _filtered_pageState createState() => _filtered_pageState();
}

class _filtered_pageState extends State<filtered_page> {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('filter'),
    );
  }
}
